import "@/app/styles/loading.css";
import PageLoadingComponent from "@/components/common/PageLoadingComponent";

type Props = {};

const Loading = (props: Props) => {
  return <PageLoadingComponent className="gradient-home" />;
};

export default Loading;
