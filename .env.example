# Missing translations are warnings by default. Set true in CI to require every
# published English entry to have all configured locale variants.
STRICT_I18N=false

# Optional external translation adapter. The executable receives one JSON
# payload on stdin and must return a complete localized MDX document on stdout.
CONTENT_TRANSLATION_COMMAND=
CONTENT_TRANSLATION_ARGS=[]
